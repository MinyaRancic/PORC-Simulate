$main $command (match “user”) (match_jmp)
$main $command (match “pass”) (match_jmp)
$main $command (match “quit”) (match_jmp)
$main $command (match “stat”) (match_jmp)
$main $command (match “list”) (match_jmp)
$main $command (match “retr”) (match_jmp)
$main $command (match “dele”) (match_jmp)
$main $command (match “noop”) (match_jmp)
$main $command (match “rset”) (match_jmp)
$command $end (match “\n”) (match_jmp)